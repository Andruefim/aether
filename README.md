## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run [Ollama](https://ollama.com) locally and pull the model: `ollama pull qwen3:latest`
3. Run the app:
   `npm run dev`
